# Example environment variables for the wx-info project
# Copy this file to `.env` and fill in any secrets or environment-specific values.

# --- Database configuration ---
DATABASE_HOST=postgres         # Hostname of the PostgreSQL server (use 'postgres' for Docker Compose)
DATABASE_PORT=5432             # Port for PostgreSQL (default: 5432)
DATABASE_USER=postgres         # PostgreSQL username
DATABASE_PASS=the-password     # PostgreSQL password
DATABASE_NAME=postgres         # Name of the PostgreSQL database

# --- Weather API configuration ---
NWS_API_BASE_URL=https://api.weather.gov   # Base URL for the National Weather Service API

# --- Weather pipeline configuration ---
WX_STATION_ID=KATL             # Weather station ID to fetch data for (default: KATL)

# --- Airflow configuration ---
AIRFLOW_UID=50000              # User ID for Airflow container file permissions (Linux only)